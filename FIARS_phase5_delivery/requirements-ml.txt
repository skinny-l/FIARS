# Optional: enables semantic/hybrid retrieval (Phase 4).
# FIARS runs fully without this file — lexical (FTS5) search only.
#
#   pip install -r requirements-ml.txt
#   python scripts/build_index.py
#
# First run downloads the ~80MB all-MiniLM-L6-v2 model once; fully offline
# after that. No FAISS/vector-DB dependency: at FIARS' scale a NumPy/pure-Python
# brute-force cosine search over the embedding table is fast enough and keeps
# the index inside the same SQLite file instead of a second file to sync.
sentence-transformers>=2.2
