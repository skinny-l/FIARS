"""
fiars.eval — retrieval quality evaluation (Phase 4).

leave_one_out.py measures whether the recommendation engine recovers the
correct historical root cause for a case when that case itself is hidden
from the search index. metrics.py holds the small, reusable scoring math.
"""
